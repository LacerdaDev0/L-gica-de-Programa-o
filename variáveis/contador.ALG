Algoritmo "ConteAteONumero"

Var

   contador, num: inteiro


Inicio

   Escreval ("Diga seu número: ")
   Leia(num)
   contador <- 0
   Enquanto (contador <= num) faca
      Escreval(contador)
      contador <- contador + 1
   FimEnquanto

Fimalgoritmo